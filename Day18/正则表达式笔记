1 正则表达式用来干嘛的？
匹配字符串的


正则表达式的方法：
1 findall():所有结果都返回到一个列表里
2 search()：返回一个对象(object),对象可以调用group()返回结果
3 match()（用的不多）: 只在字符串开始匹配匹配,也返回一个对象(object),对象也可以调用group()返回结果
4 split():分隔方法,先分前面的参数，再分后面的参数
5 sub():替换方法，三个参数，将最后一个参数其中的参数替换。旧参数，新参数，第一个参数可以是匹配规则
subn（）则是替换所有的
6 compile():匹配一个规则对象，用规则对象进行匹配，后面只有一个参数，就是陪匹配的内容
7 finditer： 迭代器，匹配到对象变成一个地址 ，输出next（ret）.group









